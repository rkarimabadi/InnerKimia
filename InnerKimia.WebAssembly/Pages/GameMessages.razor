@if (Messages.Any())
{
    <div class="game-messages" style="direction:rtl">
        @foreach (var message in Messages)
        {
            <div class="message">@message</div>
        }
    </div>
}

@code {
    [Parameter] public List<string> Messages { get; set; } = new();
}